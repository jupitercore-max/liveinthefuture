import React, { createContext } from "react";

export const HatchContextReactContext = createContext(null);

export function HatchContextProvider({ children, value }) {
  return React.createElement(HatchContextReactContext.Provider, { value }, children);
}
